const factorial = (n)  => {
   if ((n === 0) ||  (n=== 1)) {
      return 1;
    }else{
        return (n * factorial(n-1 ));
    }
}    
    console.log(factorial(33))

    
    let orderCount = 0;

    const takeOrder = (size, topping) => {
        console.log (`pizza with ${topping}, ${size}`);
        orderCount++
    }
    takeOrder ("pineapple", "14inch");

    let pinNumber = 1234;
    let Balance = 500;

    const withdrawal = (pinNumber, Balance)=> {{

        if (pinNumber == 1234 && balance <= 500)
    console.log (" password accepted and sufficiant balance!");

    else if ( balance => 501)
    console.log ("Insuficiant funds");

    else (pinNumber ) => 1234;
    console.log ("incorrect password");
    
}
checkPin (1234, 501)

withdrawal (1234, 501)
}
